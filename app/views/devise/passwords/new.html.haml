.card-content

  %h2.flow-text Forgot your password?
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .row
      .input-field.col.s12
        = f.label :email
        = f.email_field :email, autofocus: true, autocomplete: "email", class: 'validate'
    %button.btn.waves-effect.black.waves-light.white-text{type: 'submit'} Send me password reset instructions
= render "devise/shared/links"
